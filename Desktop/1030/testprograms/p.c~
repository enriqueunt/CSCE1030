#include <stdio.h>
int main (){
int num;
int row;
int col;
do {
  printf ("Please enter a number (5 to 10): ");
  scanf ("%d",&num);
  
  }while (num < 5 || num > 10);
  
  for (row=2; row<=num; row++){
  for (col=2; col<=row; col++){
  printf ("*");
  }
  printf ("\n");
  }
  
  return 0;
  }